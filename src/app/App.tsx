// Ponto de entrada do app. A partir da Fase 2, apenas monta as rotas
// (app/routes.tsx) — o placeholder da Fase 1 foi substituído pelo RoleGate.

import AppRoutes from './routes';

function App() {
  return <AppRoutes />;
}

export default App;
